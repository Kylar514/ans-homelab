# Managed by Ansible - do not edit manually
{% for key in pubkeys %}
{% if key == 'files/ansible.pub' %}
from="{{ ansible_control_ip }}" {{ lookup('file', playbook_dir + '/' + key) }}
{% else %}
{{ lookup('file', playbook_dir + '/' + key) }}
{% endif %}
{% endfor %}
