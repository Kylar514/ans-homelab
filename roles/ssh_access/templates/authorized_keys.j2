# Managed by Ansible - do not edit manually
{% for key in item.pubkeys %}
{% if key == 'ansible.pub' %}
from="{{ ansible_control_ip }}" {{ lookup('file', ssh_pubkey_path + '/' + key) }}
{% else %}
{{ lookup('file', ssh_pubkey_path + '/' + key) }}
{% endif %}
{% endfor %}
