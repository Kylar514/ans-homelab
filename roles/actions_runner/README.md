# actions_runner

Installs and configures self-hosted GitHub Actions runners on Azir.

## Variables

- `actions_runner_repos`: List of repos to register runners for
- `actions_runner_labels`: Runner labels (default: `self-hosted,Linux,X64`)
- `actions_runner_force_reregister`: Force re-registration
