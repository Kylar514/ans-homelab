# SPDX-License-Identifier: MIT-0
---
# tasks file for sys_packages

- name: Ensure required packages are installed
  ansible.builtin.package:
    name: '{{ item }}'
    state: present
    update_cache: yes
  loop: '{{ (all_packages + group_packages | default([])) | unique }}'
