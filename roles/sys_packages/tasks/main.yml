#SPDX-License-Identifier: MIT-0
---
# tasks file for sys_packages

- name: Ensure required packages are installed
  package:
    name: '{{ item }}'
    state: present
    update_cache: yes
  loop: '{{ (packages_all + packages_group | default([])) | unique }}'
